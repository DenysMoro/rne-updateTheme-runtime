import { Theme } from 'react-native-elements'

export const palette = {
  white: '#FFFFFF',
  primary: '#256E46',
  black: '#000',
}

export const theme = {
  colors: palette,
  Header: {
    backgroundColor: '#333333',
  },
} as Theme
